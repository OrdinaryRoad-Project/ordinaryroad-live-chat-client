name: Update Maven Dependency Tree

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update_maven_dependency_tree:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Submit Dependency Snapshot
        uses: advanced-security/maven-dependency-submission-action@v3
